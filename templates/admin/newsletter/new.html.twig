{% extends 'admin/layout.html.twig' %}

{% block title %}New Newsletter{% endblock %}

{% block content %}
    <div class="container">
        <h1>Create new Newsletter</h1>

        {{ include('admin/newsletter/_form.html.twig') }}

        <a href="{{ path('admin_newsletter_index') }}" class="btn btn-dark">back to list</a>
    </div>
{% endblock %}
